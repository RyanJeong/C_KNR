#include <ctype.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum {
    NAME,
    PARENS,
    BRACKETS,

    NO = 0,
    YES,
    
    MAXTOKEN = 100,
    MAXLEN   = 1000
};

int main(void)
{

    return 0;
}
